[package]
name = "gpupatch-node"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "2.16.0", features = ["napi4"] }
napi-derive = "2.16.0"
gpupatch-core = { path = "../core" }

[build-dependencies]
napi-build = "2.1.0"
